const fs = require('fs');
const path = require('path');

function handlerClosure(name){
  var filePath = './views/' +  name;
  return function(res){
    fs.readFile(filePath, (err, data) => {
      res.writeHead(200, { 'Content-Type': 'text/html' });
      res.end(data);
    });
  };
}

